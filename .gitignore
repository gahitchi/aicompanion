__pycache__/
*.pyc
*.pyo
.venv*/
venv/
.env

# build / packaging artifacts
*.egg-info/
build/
dist/

# heavy ASR models
models/

# Chroma persistent store
memory_db/

# runtime state (private user data)
emotion.json
identity_memory.json
episodes.json
user.json
followups.json
consolidator_state.json
reminders.json
system.log
temp.wav

# enrolled voiceprint (biometric — never commit)
voiceprint.npz
